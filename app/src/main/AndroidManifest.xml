<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.engazadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EngazAdmin">
        <activity android:name=".Branches.BranchAddActivity"></activity>
        <activity android:name=".Services.ServicesActivity"/>
        <activity android:name=".Services.AddServicesActivity"/>
        <activity android:name=".Branches.BranchActivity" />
        <activity android:name=".ServicesInfo.InfoActivity"/>
        <activity android:name=".ServicesInfo.AddInfoActivity"/>
        <activity android:name=".Companies.AddCompaniesActivity" />
        <activity android:name=".Auth.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Auth.SignUpActivity" />
        <activity
            android:name=".MainActivity"
            />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

    </application>

</manifest>